<div class="container">
  <h3>Create a new list</h3>

  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name %>
    <%= f.submit %>
  <% end %>

  <%= link_to "Back to my lists", lists_path %>
</div>
